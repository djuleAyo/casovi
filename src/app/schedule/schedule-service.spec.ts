import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { Schedule } from './schedule.model';

describe('Schedule', () => {
    describe('toDateArray', () => {
        it('should return array of all dates that match given cron pattern and are in given time interval', () => {
            // arrange
            // act
            // assert
            expect(1).toBe(1);
        });
    });
    describe('union', () => {
        it('should return union (as set operaiton) of given arrays', () => {
            // arrange
            // act
            // assert
        });
    });
    describe('diff', () => {
        it('should return diff (as set operaition) of given arrays', () => {
            // arrange
            // act
            // assert
        });
    });
    describe('splitByDate', () => {
        it('should split given array by date basis in multiple arrays', () => {
            // arrange
            // act
            // assert
        });
    });
    describe('toIntervals', () => {
        it('shoud unify same dates which have same statuses and distance is equal to configuration.timeDelta into an interval', () => {
            // arrange
            // act
            // assert
            expect(1).toBe(1);
        });
    });
});
